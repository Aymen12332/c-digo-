@@ .. @@
   "scripts": {
     "dev": "NODE_ENV=development tsx server/index.ts",
-    "build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
+    "build": "npm run build:client && npm run build:server",
     "build:client": "vite build --outDir dist/client",
-    "build:admin": "vite build --outDir dist/admin",
-    "build:delivery": "vite build --outDir dist/delivery",
-    "build:all": "npm run build:client && npm run build:admin && npm run build:delivery",
+    "build:server": "esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist --target=node18",
     "start": "NODE_ENV=production node dist/index.js",
     "check": "tsc",
     "db:generate": "drizzle-kit generate",
     "db:push": "drizzle-kit push",
-    "db:setup": "ts-node migrations/scripts/setup-database",
-    "db:reset": "drizzle-kit drop && npm run db:setup"
+    "postinstall": "npm run db:push"